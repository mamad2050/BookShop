<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:layoutDirection="rtl"
    android:padding="8dp">

    <RelativeLayout
        android:id="@+id/activity_question_parent_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_title_question"
        android:padding="8dp">

        <TextView
            android:id="@+id/activity_question_txt_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginEnd="8dp"
            android:layout_toStartOf="@id/activity_question_img"
            android:maxLines="1"
            android:text="عنوان"
            android:textColor="@color/colorPrimary"
            android:textSize="12sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/activity_question_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_baseline_add_24" />

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/activity_question_parent_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/activity_question_parent_title"
        android:layout_marginTop="-8dp"
        android:background="@drawable/bg_description_question"
        android:padding="8dp"

        android:visibility="gone">


        <TextView
            android:id="@+id/activity_question_txt_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingExtra="4dp"
            android:textColor="@color/black"
            android:textSize="13sp" />


    </RelativeLayout>


</RelativeLayout>